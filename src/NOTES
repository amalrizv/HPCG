// Each receive-from neighbor should be a send-to neighbor, and send the same number of entries

This is not happening. Sets(values) for Dictionary receiveList and sendList are different.
Setup_halo_ref line 109 110 in cpp
